Import('RTT_ROOT')
Import('rtconfig')
from building import *

objs = []
cwd     = GetCurrentDir()
list = os.listdir(cwd)
src     = Glob('*.c') + Glob('*.cpp')
CPPPATH = [cwd, str(Dir('#'))]

group = DefineGroup('cpu_load', src, depend = ['LPKG_USING_CPU_LOAD'], CPPPATH = CPPPATH)

Return('group')
